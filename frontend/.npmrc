# Wails runs `npm install` before starting the frontend dev server. Avoid making
# startup depend on the npm audit service, which can delay Vite long enough for
# Wails' frontend health check to time out.
audit=false
fund=false

# Only resolve package versions that have been available for at least 7 days.
min-release-age=7
